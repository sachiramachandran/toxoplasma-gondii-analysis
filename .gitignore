# macOS
.DS_Store
._*

# Python
__pycache__/
*.py[cod]

# Snakemake state
.snakemake/

# Pipeline output — regenerate with `nextstrain build --docker .`
toxo-analysis/results/
toxo-analysis/logs/

# NOTE: auspice/ at the repository root is intentionally tracked.
# nextstrain.org/community serves auspice/toxoplasma-gondii-analysis.json
# directly from the default branch, so it must stay in version control.
